;; app-maps — maps-tile-server appview の Worker。
;;
;; 入口は ClojureScript。superproject CLAUDE.md の runtime 順序
;; (kotoba wasm > clojurewasm > ClojureScript > nbb) に従う。入口を .kotoba に
;; しないのは ADR-2606290000 の判断で、kotoba の ingress capability が
;; :native-aot / :wasm-aot とも pending だからである。判断の形をした部分を
;; .cljc に置いてあるのは、ブラウザもビルドも無しに検査するためであり、
;; ingress が qualify した時に最初に .kotoba へ移る部分だからでもある。
;;
;; この deps.edn が担当するのは src/maps/tile/** だけである。この repo は
;; 4 つの subsystem を同居させており（README.md 参照）、kotoba/ の TypeScript・
;; bulk-ingest/ の Python・maps-ui appview はここに載らない。
{:paths ["src"]
 :deps {
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/jp-go-digital-design-system
        {:git/url "https://github.com/kotoba-lang/jp-go-digital-design-system"
         :git/sha "2e2d191e9e1731ce6865c79dab163a5d74249053"}}
 :aliases
 {:cljs {:extra-deps {org.clojure/clojurescript {:mvn/version "1.11.132"}
                      thheller/shadow-cljs {:mvn/version "2.28.20"}}}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}}}
